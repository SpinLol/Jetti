mutation AddPlayer($userId: String, $level: Float, $userTag: String) {
  newPlayer: createPlayer(data: { userId: $userId, skillLevel: $level, userTag: $userTag }) {
    id
    userTag
    skillLevel
  }
}
